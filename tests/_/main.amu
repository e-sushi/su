main :: () -> void {
	a: (u32, #compiler_break u32) = (1,1);
}

Player :: struct { 
	health: u32;
	mana: u32;

	flags: StatusFlags;
}

StatusFlags :: struct {
	is_bleeding: u32;
	is_poisoned: u32;
}

