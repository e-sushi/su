StatusFlags :: struct {
    is_poisoned: u32;
    is_bleeding: u32;
}

Player :: struct {
    health: u32;
    mana: u32;

    status: StatusFlags;
}

width :: 5;
height :: 5;

main::()->void {
    b :: 1;
    arr: u8[width * height];
    a := b;
    :b = 10;
    c := b + 2;
}