vec2 :: struct {
    x: f32;
    y: f32;
}

add :: (a: vec2, b: vec2) -> vec2 {
    out: vec2;
    out.x = a.x + b.x;
    out.y = a.y + b.y;
    out
}

main :: () -> void {
    a: vec2;
    b: vec2;
    a.x = 1.2;
    a.y = 3.2;
    b.x = a.x + 1.2;
    b.y = 3.2;

    a = add(a, add(b,b));
}
