#import "tests/_/math.amu"

main() : s32 {
    sin(1.2);
    i : mint = {
        sign = -1,
        count = 2,
        mag = 0x213,
    };
    i.mag;
    i.sign;
    i.count;

    hey : str = sin(1.2);
}