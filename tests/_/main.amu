StatusFlags :: struct {
    is_poisoned: u32;
    is_bleeding: u32;
}

Player :: struct {
    health: u32;
    mana: u32;

    status: StatusFlags;
}

main::()->void {
    d := :{
        a := 1;
        b := 2;
        c := a + b;
        c + 3
    }
}